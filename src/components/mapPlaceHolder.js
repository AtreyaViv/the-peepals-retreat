import React from "react";

const MapPlaceholder = () => {
  return (
    <div className="map-placeholder">
      <button className="map-button">SEE MAP 📍</button>
    </div>
  );
};

export default MapPlaceholder;